# JavaScript Kata Init

Provides a skeleton for doing katas in JS - using [tape](https://github.com/substack/tape)
Supports ES6 syntax

Run tests once

    npm test

Rerun tests automatically on changes

    npm run test-watch
